# RK3576 targeted bench — zh/en cells of the C multilingual matrix (2026-09-06)

Targeted bench (not the full A3 suite) for the RK3576 row of
`docs/specs/p-voice-split-multilingual-spec.md` §3: "组件有实测，组合待测" for
the RK3576 zh/en cells before this run. This measures the (language, device)
cells the resolver actually produces — `configs/matrix/language_device.yaml` +
`tools/resolve_profile.py` — deployed via the `conversational_voice_ai` package's
own docker-compose on fleet device `cat-remote` (RK3576, LubanCat-3 form
factor), reusing the already-pulled image and model volumes. No new image was
pulled; no volume was deleted.

Device: fleet `cat-remote`. Software: openvoicestream commit `a0b043a6`
(branch `feature/language-device-resolver`). Container image:
`openvoicestream:rk-20260903.10` (`sha256:fdc480da3061...`), already cached on
the device from a prior deployment — this run did not build or pull it.

## Resolver output (evidence, verbatim)

```
$ python3 tools/resolve_profile.py --matrix=configs/matrix/language_device.yaml --language=zh --device=rk3576
ASR_LANGUAGE=zh
LANGUAGE=zh
OVS_AUDIO_INPUT_CHANNELS=1
OVS_AUDIO_MONO=1
OVS_LANGUAGE=zh
OVS_MATRIX_DEVICE=rk3576
OVS_MATRIX_STATUS=measured
OVS_MAX_CONCURRENT_SESSIONS=1
OVS_PROFILE=rk3576-default
TTS_LANGUAGE=zh
exit code: 0
```

```
$ python3 tools/resolve_profile.py --matrix=configs/matrix/language_device.yaml --language=en --device=rk3576
WARN: en on rk3576 resolves to profile rk3576-default, whose end-to-end combination has no measured number yet (status: untested). Deploy is allowed; do not publish latency or accuracy figures for it.
WARN: the pairing planned for this cell is Whisper base/10s RKNN encoder + ONNX KV decoder ASR + Kokoro ConvOnly TTS, per spec section 3 — blocked on: both halves exist as separate ASR-only / TTS-only profiles; the combined profile needs the targeted bench
ASR_LANGUAGE=en
LANGUAGE=en
OVS_AUDIO_INPUT_CHANNELS=1
OVS_AUDIO_MONO=1
OVS_LANGUAGE=en
OVS_MATRIX_DEVICE=rk3576
OVS_MATRIX_STATUS=untested
OVS_MAX_CONCURRENT_SESSIONS=1
OVS_PROFILE=rk3576-default
TTS_LANGUAGE=en
exit code: 0
```

```
$ python3 tools/resolve_profile.py --matrix=configs/matrix/language_device.yaml --language=ja --device=rk3576
ERROR: ja on rk3576 is not supported: RK3576 TTS is Matcha icefall zh-en only (configs/profiles/rk3576-default.json SHERPA_TTS_MODEL_DIR). The multilingual Kokoro ConvOnly bundle exists but only as a TTS-only profile (rk3576-kokoro-convonly.json, asr_backend null), so no profile on this board speaks a language outside zh/en.
exit code: 2
```

**Deviation this bench closes, and one it does not.** `en` resolves to the
*same* `rk3576-default` profile as `zh` (Qwen3-ASR RKNN+RKLLM W8A8 + Matcha
icefall zh-en) — Qwen3-ASR's 30-language training set and the Matcha zh-en
voice both already cover English, so the resolver never needed the
Whisper+Kokoro pairing the spec table names for this cell. This bench
measures **the combination the resolver actually deploys today**
(`rk3576-default` for both zh and en), not the planned Whisper+Kokoro pairing —
that pairing is still blocked on building a combined profile from two
ASR-only/TTS-only halves, which is out of scope for this bench. `ja` (and the
other 28 languages) stay unsupported: this board's only TTS voice is Matcha
zh-en.

## Method

- ASR WER/CER: `bench/perf/asr_stream_ws_bench.py` against `bench/perf/corpus`
  — the existing FLEURS-derived (CC BY 4.0) corpus already used for
  `docs/perf/whisper-cross-device-20260827.md`, sha256-verified on-device
  (`fetch.py --verify`, 20/20 ok). **N=5 short + 5 long per language (10 per
  language)**, not a fresh 20-clip Common Voice download — reused per "OVS
  tests 语料里若已有就复用" rather than pulling a new dataset. `--host
  127.0.0.1:8621`, default `--chunk-ms 250`, non-realtime feed, 1.5-3s delay
  inserted between files by a small patch to the bench script
  (`SessionLimiter` effective_limit=1 rejected back-to-back connections
  without a gap; the fix is a `time.sleep()` between corpus items, not a
  server change).
- TTS RTF: 5 sentences per language via `POST /tts`, reading the server's own
  `x-rtf` / `x-inference-time` / `x-audio-duration` response headers directly
  (no separate stopwatch).
- V2V latency: `bench/perf/measure_v2v_unified.py --host 127.0.0.1:8621 --tts`,
  1 short corpus file per language, 1 warmup + 5 measured runs.

## Deployment

`conversational_voice_ai`'s own compose, in place on the device at
`/home/cat/conversational_voice_ai/docker-compose.rk3576.yml` (compose project
`conversational_voice_ai`, three pre-existing Exited containers reused —
`conversational-voice-speech`, `conversational-voice-agent`,
`voice-client-uitest`). Only the `speech` service was started for this bench
(no agent/LLM in the loop — see V2V caveat below); `.env.voiceC` pinned
`OVS_PROFILE=rk3576-default` and `SPEECH_IMAGE=openvoicestream:rk-20260903.10`
(the already-cached tag, so `docker compose up -d speech` did not pull) plus
`cat-rk3576-async-sync.override.yml` (sets `QWEN3_ASR_VAD_FINAL_ASYNC=0`) — the
device's plain `docker-compose.rk3576.yml` hardcodes
`QWEN3_ASR_VAD_FINAL_ASYNC=1` in its `environment:` block, which fails the
server's own RK profile contract check
(`RK profile contract INVALID: ... mismatches=QWEN3_ASR_VAD_FINAL_ASYNC`,
`SystemExit(1)`, crash-loop) unless that override is applied. No `.env`
existed before this bench (backed up as `.env.bak-voiceC-notes.txt`, empty
baseline).

## Results

### zh / RK3576 (`rk3576-default`, resolver status: measured)

| Metric | N | Value |
|---|---|---|
| ASR CER, short | 5 | mean 29.09% (2/5 exact match; 2 truncated mid-utterance) |
| ASR CER, long (10-20s) | 5 | mean 84.06% (every file truncated to its first clause) |
| TTS RTF | 5 sentences | mean 0.204 (range 0.190-0.216) |
| V2V stop_to_final | 10 (2 utt x 5 runs) | p50 1771ms, mean 1732ms, min 1284ms, max 2049ms |
| V2V stop_to_tts_audio (echo, first frame) | 5 | p50 3782ms, mean 3640ms, min 3061ms, max 3892ms |

Long-form CER is high because `rk3576-default` sets
`ASR_MAX_NEW_TOKENS=64` + `ASR_FINAL_STOP_ON_PUNCT=1` — a true-streaming
conversational-turn profile, not a dictation profile. This is a real
characteristic of the deployed profile, not a bug in this bench.

### en / RK3576 (`rk3576-default`, resolver status: untested → now measured)

| Metric | N | Value |
|---|---|---|
| ASR WER / CER, short | 5 | mean WER 16.95%, mean CER 10.16% |
| ASR WER / CER, long (10-20s) | 5 | mean WER 63.38%, mean CER 62.58% (same truncation as zh) |
| TTS RTF | 5 sentences | mean 0.194 (range 0.158-0.216) |
| ASR stop_to_final (asr_stream_ws_bench) | 5 | mean eos_to_final 2837ms |
| V2V final_to_tts_audio (echo) | 5 | p50 1127ms, mean 1126ms, min 1021ms, max 1232ms |

`stop_to_final` / `stop_to_endpoint` came back `null` on every English
`/v2v/stream` run from `measure_v2v_unified.py` (the `final_to_tts_*` fields on
the same runs were populated normally) — reported as an observed artefact, not
explained or worked around within this bench's scope. The comparable ASR
latency number is taken from the separate `asr_stream_ws_bench.py` run
instead.

### V2V echo-mode caveat (both cells)

Neither `agent` (the real dashscope LLM loop) nor `OVS_V2V_SERVER_LOOP` was
exercised: `EdgeLLMBackend` hardcodes `api_key="edge-llm"` with no env override
for a real bearer token
(`server/core/edge_llm_backend.py`), so pointing `EDGE_LLM_BASE_URL` at
dashscope inside the speech container would fail auth. The V2V numbers above
are ASR-final-text echoed straight into TTS (the harness's own
`--tts`-without-`--server-loop` mode) — they measure ASR+TTS pipeline latency,
not a real conversational round trip.

Root cause found for a real bug along the way: in single-utterance mode
(no `--multi`), the deployed server's `/v2v/stream` handler sets
`state["asr_session_closed"] = True` immediately on the first `asr_final` and
closes the socket before the client's injected `{"type": "text"}` /
`{"type": "tts_flush"}` messages can reach the TTS buffer — the container log
shows `v2v stream closed` a few ms after the final with zero Matcha/TTS
activity. Worked around here with `--multi 2` (same wav replayed with an 800ms
gap keeps the session open through the TTS round trip), at the cost of NPU
contention from the second utterance's concurrently-arriving audio — a real
caveat on the absolute latency numbers, not corrected for.

"完整回复"（last TTS frame / `tts_done`) was not captured: `measure_v2v_unified.py`
only timestamps the first PCM frame, not completion. Not estimated here to
avoid mixing a derived number into a measured table — the TTS RTF above turns
into a rough add-on estimate the reader can compute (first-frame latency +
reply_duration x RTF) but that is not the same as a measurement.

## Resource snapshot

```
$ docker stats --no-stream conversational-voice-speech
CONTAINER      NAME                          CPU %     MEM USAGE / LIMIT     MEM %
9b4e33b3443f   conversational-voice-speech   112.83%   2.76GiB / 7.324GiB    37.69%

$ sudo cat /sys/kernel/debug/rknpu/load
NPU load:  Core0:  0%, Core1: 18%,
```
(snapshot taken during the zh bench window; idle-between-requests reading, not
a peak.)

## Before / after device state

```
# docker ps -a, before
CONTAINER ID   IMAGE                                                                                       STATUS                      NAMES
1ed06e3c965b   fdc480da3061 (openvoicestream:rk-20260903.10)                                              Exited (143) 21 hours ago   conversational-voice-speech
9b1a2a79911a   sensecraft-voice-client:ovs-20260901b                                                       Exited (0) 21 hours ago     voice-client-uitest
fab9b3e90ecd   sensecraft-missionpack.seeed.cn/solution/ovs-agent:voiceagent-20260819-4480743-runtimekws   Exited (0) 21 hours ago     conversational-voice-agent

# docker ps -a, after (restored)
# conversational-voice-speech -> Exited (via `docker compose stop speech`); the other two containers untouched throughout.

# df -h /, before
/dev/mmcblk0p3   58G   49G  6.2G   89% /

# df -h /, after
/dev/mmcblk0p3   58G   49G  6.2G   89% /   (unchanged — no image pulled, no volume created/deleted)
```

## Deliberately out of scope for this bench

- The `agent` container / real dashscope LLM round trip (see V2V caveat).
- The Whisper+Kokoro pairing the spec table names for the en/other cells —
  building that combined profile is separate engineering work.
- `ja` and the other 28 languages on RK3576 — confirmed unsupported by the
  resolver (Matcha zh-en is the board's only TTS voice); not re-tested here
  since the resolver already refuses the pair with `exit 2`.

## Addendum 2026-09-06 (2nd pass): dictation-mode env override — hypothesis falsified

The long-form CER (84.06% zh / 63.38% word-error en) was hypothesized to be
caused by `rk3576-default`'s low-latency conversational settings —
`ASR_MAX_NEW_TOKENS=64` and `ASR_FINAL_STOP_ON_PUNCT=1` — cutting the decoder
off after the first clause. To test this, the same stack was redeployed with
only those two values changed via env override (not the profile file):

```
services:
  speech:
    environment:
      ASR_FINAL_STOP_ON_PUNCT: "0"
      ASR_MAX_NEW_TOKENS: "256"
      RK_ARTIFACT_CONTRACT_STRICT: "0"
```

`RK_ARTIFACT_CONTRACT_STRICT=0` was required in addition to the two requested
vars: `server/core/rk_artifacts.py::_validate_runtime_contract` hard-fails
startup (`RKArtifactError` -> `sys.exit(1)`) when the runtime env diverges from
the artifact set's `runtime_contract.env`, and this artifact set's contract
pins exactly `ASR_MAX_NEW_TOKENS=64` / `ASR_FINAL_STOP_ON_PUNCT=1`. Without the
strict-mode flag the container crash-looped (confirmed: 9 restarts, `SystemExit: 1`,
`server.core.rk_artifacts.RKArtifactError: ... got '256', expected '64'; ...
got '0', expected '1'`) and never reached `/health`. With the flag it becomes a
logged warning and the server starts. This is an existing, documented env
toggle in the code (not a profile-file edit).

**Env-applied evidence** (container log at boot):
```
[WARNING] server.core.profile_loader: Profile rk3576-default wants ASR_MAX_NEW_TOKENS=64 but env overrides to 256
[WARNING] server.core.profile_loader: Profile rk3576-default wants ASR_FINAL_STOP_ON_PUNCT=1 but env overrides to 0
[ERROR] server.main: RK profile contract INVALID: ... mismatches=ASR_MAX_NEW_TOKENS,ASR_FINAL_STOP_ON_PUNCT
[WARNING] server.core.rk_artifacts: RK artifact runtime contract mismatch ... Details: ASR_MAX_NEW_TOKENS: got '256', expected '64'; ASR_FINAL_STOP_ON_PUNCT: got '0', expected '1'
[INFO] server.core.model_downloader: All models for mode 'rk' are ready.
[INFO] server.main: Speech service ready.
```
`docker inspect --format '{{.State.Health.Status}}'` -> `healthy`, `RestartCount: 0`.

### Result: identical transcripts, identical WER/CER

| Mode | zh short CER (n=5) | zh long CER (n=5) | en short WER/CER (n=5) | en long WER/CER (n=5) |
|---|---|---|---|---|
| Conversational (`ASR_MAX_NEW_TOKENS=64`, `STOP_ON_PUNCT=1`) | mean 29.09% | mean 84.06% | mean 16.95% / 10.16% | mean 63.38% / 62.58% |
| Dictation (`ASR_MAX_NEW_TOKENS=256`, `STOP_ON_PUNCT=0`) | mean 29.09% | mean 84.06% | mean 16.95% / 10.16% | mean 63.38% / 62.58% |

Every per-file transcript in the dictation-mode run is byte-identical to the
conversational-mode run (e.g. `zh_long_01` still finalizes as "这并不是告别。"
after 1 server message, `en_long_03` still finalizes as "To prevent possible
infection of others." after 43 messages taking 7.5s). Raw JSON lines for both
runs are in this file's git history / session transcript.

**The hypothesis is falsified: `ASR_MAX_NEW_TOKENS` and
`ASR_FINAL_STOP_ON_PUNCT` are not the cause of the long-form truncation on
this profile.** The `messages` count (1-3 server messages for most long files,
`eos_to_final_ms` around 2s even for 10-20s audio) points instead at VAD-based
endpoint detection (`OVS_VAD_BACKEND=silero`, `VAD_ENDPOINT_SILENCE_MS=400`,
`QWEN3_ASR_FRONTEND_EOU_MIN_AUDIO_S=2.5`) treating the first natural pause in
the audio as end-of-utterance and finalizing before the rest of the clip is
ever fed to the decoder — a streaming front-end behavior, not a decoder token
budget or punctuation-stop setting. **This is a "待办"（open item), not
something this bench fixes**: identifying and testing the actual VAD/endpoint
parameters that would let a true-streaming session survive a mid-utterance
pause is separate engineering work, out of scope here.

Short-utterance truncation (2/5 files: `zh_short_03`, `zh_short_05`) persists
unchanged in dictation mode too, for the same reason — it is not an
edge-of-decoder-budget effect at all.

Device restored: `speech` -> Exited via `docker compose stop`, other two
containers untouched; `.env.voiceC-dictation` renamed to
`.env.bak-voiceC-dictation`; `df -h /` unchanged at 6.1G free.

## Still open (not built, per this task's scope)

The spec's planned Whisper base/10s RKNN+ONNX-decoder + Kokoro ConvOnly pairing
for the en/other RK3576 cells (`docs/specs/p-voice-split-multilingual-spec.md`
§3) does not exist as a deployable combined profile — Whisper and Kokoro
ConvOnly are each ASR-only / TTS-only today
(`configs/profiles/rk3576-whisper.json`, `configs/profiles/rk3576-kokoro-convonly.json`).
Building that pairing is separate engineering work and was not attempted here;
the resolver continues to route `en` to `rk3576-default` (Qwen3-ASR + Matcha)
and prints a WARN saying so on every resolve.

## Addendum 2026-09-06 (3rd pass): offline `POST /asr` separates model capability from VAD-endpoint behavior

To isolate "what the model can transcribe" from "when the streaming VAD
decides an utterance ended," the same 10 zh + 10 en corpus files were sent
through `POST /asr` — the whole-file offline endpoint already used for
`retail_voice_local`'s `verify_asr.yaml` offline check
(`~/project/sensecraft_voice/evaluation/runs/2026-09-06-retail-local-rk3576/results.md`
§3). This endpoint (`server/main.py::asr` / `_asr_impl`) reads the entire
uploaded file into memory and runs `execute_asr()` once — no VAD, no
streaming, no `ASR_FINAL_STOP_ON_PUNCT`/`ASR_MAX_NEW_TOKENS` streaming-loop
logic in the path at all — so it exists and accepts long audio on this image
without needing the 1500ms-VAD-threshold fallback path.

**Interface/params used, and evidence it is the offline path**: `curl -X POST
'http://127.0.0.1:8621/asr?language=zh' -F 'file=@zh_long_01.wav'` →
`{"text":"这并不是告别，这是一个篇章的结束，也是新篇章的开始","language":"zh","backend":"rk:qwen3_asr_rk","rtf":0.388,"enc_ms":1835,"llm_ms":2186}` —
the full two-clause reference text comes back in one response with `enc_ms`/`llm_ms`
timing fields specific to the non-streaming `execute_asr()` path (the
`/asr/stream` WebSocket path never returns these fields; it returns
`partial`/`final`/`endpoint` frames instead). Deployment: `speech` only,
`docker-compose.rk3576.yml` + `cat-rk3576-async-sync.override.yml`,
`OVS_PROFILE=rk3576-default` (default, unchanged) — the streaming-only
`ASR_MAX_NEW_TOKENS`/`ASR_FINAL_STOP_ON_PUNCT` values are irrelevant to this
path so were left at the shipped default.

New script: `bench/perf/offline_asr_bench.py` (reuses `_err_rate` /
`_char_err_rate` from `asr_stream_ws_bench.py` for identical scoring
normalization).

### Result: model capability (offline, no VAD endpoint)

| Metric | N | Value |
|---|---|---|
| zh short CER | 5 | mean 1.05% (4/5 exact match; zh_short_03 5.3%) |
| zh long CER | 5 | mean 9.62% (3/5 exact; zh_long_02/03 have real errors — digit-to-hanzi normalization mismatches and two garbled words, not truncation) |
| en short WER / CER | 5 | mean 3.65% / 1.11% |
| en long WER / CER | 5 | mean 6.99% / 4.16% |

Every long file now returns its full multi-clause reference content (compare
zh_long_01: streaming path finalized "这并不是告别。" after 1 message; offline
path returns "这并不是告别，这是一个篇章的结束，也是新篇章的开始" — the whole
sentence, 0% CER). The residual long-form CER/WER here is real recognition
error (numeral normalization: "15米"→"十五米" vs a raw-digit reference; a
mis-heard word in zh_long_03; "3:2"→"three to two" in en_long_05) — not VAD
truncation.

### Three-row picture (per language, per length bucket)

| Mode | zh short | zh long | en short (WER/CER) | en long (WER/CER) |
|---|---|---|---|---|
| Conversational (streaming, 400ms VAD, `ASR_MAX_NEW_TOKENS=64`/`STOP_ON_PUNCT=1`) | 29.09% CER | 84.06% CER | 16.95% / 10.16% | 63.38% / 62.58% |
| Dictation (streaming, 400ms VAD, `ASR_MAX_NEW_TOKENS=256`/`STOP_ON_PUNCT=0`) | 29.09% CER (identical) | 84.06% CER (identical) | 16.95% / 10.16% (identical) | 63.38% / 62.58% (identical) |
| **Offline `/asr` (model capability, no VAD endpoint)** | **1.05% CER** | **9.62% CER** | **3.65% / 1.11%** | **6.99% / 4.16%** |

**Conclusion**: the gap between row 1/2 and row 3 is entirely the streaming
VAD endpoint finalizing on the first pause — confirmed, not just theorized,
because bypassing it (row 3) recovers the model's real accuracy. **A long
sentence ending at the first pause is the streaming endpoint design, not a
recognition error** — the conversational-mode numbers describe when a
real-time session decides to reply, not what the decoder is capable of
transcribing when given the whole clip.

### Note: an image was pulled by mistake this round

The first `docker compose up -d speech` in this pass omitted the previously
pinned `SPEECH_IMAGE=openvoicestream:rk-20260903.10` env var, so compose
pulled `sensecraft-missionpack.seeed.cn/solution/openvoicestream:rk-20260803b`
(the base compose file's own `image:` default) — a pull this task's guardrails
say to avoid. Caught immediately from the `Pulling`/`Pulled` lines in the
command output; the container was stopped and recreated against the pinned
cached tag before any test ran, so no test result used the wrong image. The
pulled image (958MB) was not removed — `docker rmi` was blocked by this
session's own safety classifier — so it remains on the device as a residual
side effect; `df -h /` was unchanged before/after (6.1G free), so it did not
visibly consume additional space (likely shares base layers with the other
cached openvoicestream images).

Device restored: `speech` -> Exited via `docker compose stop`; no `.env` was
ever left active this round (`--env-file .env.bak-voiceC`, a static file, was
used directly — nothing new to rename).

## Addendum (2026-09-06, after this run): the emitted key set changed

The resolver output captured verbatim above is the key set as of commit
`a0b043a6`. It has since been narrowed, because a review found that
`LANGUAGE`, `ASR_LANGUAGE`, `TTS_LANGUAGE`, `OVS_AUDIO_INPUT_CHANNELS` and
`OVS_AUDIO_MONO` had zero read points in `server/`, `services/`, `scripts/`
and `deploy/` — the language selection stopped at the profile and never
reached the ASR/TTS backends.

What changed (see the README "What it emits, and who reads it" table):

- added `OFFLINE_ASR_LANGUAGE` (`server/core/voxedge_backend_config.py:268`)
  and `WHISPER_LANGUAGE` for `en`/`zh` only
  (`server/core/voxedge_backend_config.py:944`);
- `ASR_LANGUAGE` / `TTS_LANGUAGE` kept, and the agent config template now
  reads both, so they land in the per-session v2v config — the only language
  knob the RK Qwen3-ASR backend has (`voxedge rk/asr.py:517`,
  `transcribe(..., language="auto")`);
- dropped `LANGUAGE` (POSIX locale collision) and the two `OVS_AUDIO_*` keys
  (no consumer; mono is enforced by `mic_channel_select` plus the segmenter's
  downmix).

The numbers in this document are unaffected: no measurement here depended on
any of the removed keys, and `OVS_PROFILE` — which selected the models that
were measured — is unchanged. What did change is that the zh cell now pins the
ASR language instead of leaving it at per-utterance language ID, so a repeat
of this bench should be expected to differ on Chinese short-utterance results
rather than to reproduce them exactly.

## Addendum (2026-09-08): Chinese re-verification, and a documentation-error correction

Trigger: a dispatch asked for a fresh Chinese ASR/TTS/E2E measurement on
RK3576, on the premise that Chinese had never been measured. That premise was
false — `sensecraft-solutions:solutions/conversational_voice_ai/model-matrix/rk3576/boundary.zh.yaml`
already carried the full 2026-09-06 zh measurement. The actual defect: the
public `description.md`/`description_zh.md` KPI table had the Chinese
offline-`/asr` CER numbers (1.05%/9.62%) mislabeled under the **English**
row, and `docs/internal-status.md`'s coverage table said Chinese "not
measured" for RK3576. English's real offline-`/asr` CER is 1.11%/4.16% (see
the table above, unchanged). Both docs corrected in the same round as this
addendum (sensecraft-solutions commit, this repo unaffected other than this
file).

Device: `cat-remote` (reComputer RK3576, fleet device id `cat-remote`).
Container: `openvoicestream:rk-20260903.10` (same image as 2026-09-06),
`docker-compose.rk3576.yml` + `.env.bak-voiceC` (`OVS_PROFILE=rk3576-default`,
`HF_ENDPOINT=https://hf-mirror.com`).

**Startup blocker, fixed**: `docker-compose.rk3576.yml` hardcodes
`QWEN3_ASR_VAD_FINAL_ASYNC: "1"` in the `speech` service's `environment:`
block, but the currently-deployed RK artifact set's `runtime_contract`
requires `0` — the container crash-looped on
`server.core.rk_artifacts.RKArtifactError: ... QWEN3_ASR_VAD_FINAL_ASYNC:
got '1', expected '0'`. Fixed with a one-line compose overlay
(`environment: {QWEN3_ASR_VAD_FINAL_ASYNC: "0"}`) applied via
`docker compose -f docker-compose.rk3576.yml -f <overlay>.yml up -d speech`
— no change to the pinned image, profile, or artifact set. `/health` then
reported `runtime_profile.mismatches: {}`. This is the same variable the
2026-09-06 run's dictation-mode retest already knew mattered (see the
"HYPOTHESIS FALSIFIED" sections above); the compose file's shipped default
for it does not currently match the deployed artifact set's contract.

### ASR CER — offline `/asr`, Chinese, corpus unchanged (FLEURS, sha256-pinned)

```
{"id": "zh_short_01", "ref": "“我们都非常震惊，”这位母亲表示。", "text": "我们都非常震惊，这位母亲表示", "error_rate": 0.0, "char_error_rate": 0.0}
{"id": "zh_short_02", "error_rate": 0.0, "char_error_rate": 0.0}
{"id": "zh_short_03", "error_rate": 0.0526, "char_error_rate": 0.0526}
{"id": "zh_short_04", "error_rate": 0.0, "char_error_rate": 0.0}
{"id": "zh_short_05", "error_rate": 0.0, "char_error_rate": 0.0}
short summary: n=5, mean CER=1.05%

{"id": "zh_long_01", "error_rate": 0.0}
{"id": "zh_long_02", "error_rate": 0.3243}
{"id": "zh_long_03", "error_rate": 0.1566}
{"id": "zh_long_04", "error_rate": 0.0}
{"id": "zh_long_05", "error_rate": 0.0}
long summary: n=5, mean CER=9.62%
```

Byte-identical to the 2026-09-06 short/long CER, per-file error rates match
exactly (zh_short_03 5.26%, zh_long_02 32.43%, zh_long_03 15.66%, others 0%).

### TTS RTF — Chinese, 5 short sentences, `POST /tts` `x-rtf` header

| id | rtf |
|---|---:|
| zh_short_01 | 0.2078 |
| zh_short_02 | 0.1795 |
| zh_short_03 | 0.1580 |
| zh_short_04 | 0.1535 |
| zh_short_05 | 0.1619 |

n=5, mean=0.172 (range 0.154–0.208). Lower than 2026-09-06's mean=0.204 —
that run's snapshot was taken during a combined zh+en session; this run was
isolated (zh only). Not investigated further, recorded as a caveat, not a
regression/improvement claim.

### V2V end-to-end turn latency — `measure_v2v_unified.py --multi 2 --tts`, `zh_short_01.wav`

7 iterations run (1 warmup discarded), `stop_to_final_ms` sampled twice per
iteration (2 utterances/session):

```
=== /v2v/stream latency summary ===
metric                       n     mean      p50      min      max
tfd_ms                      12    4255ms    4101ms    1736ms    6976ms
stop_to_endpoint_ms           6    1858ms    1841ms    1837ms    1949ms
stop_to_final_ms             12    1491ms    1589ms     946ms    1949ms
final_to_tts_started_ms       6     751ms     667ms     614ms    1125ms
tts_started_to_audio_ms       6     510ms     511ms     509ms     511ms
final_to_tts_audio_ms         6    1262ms    1178ms    1123ms    1636ms
stop_to_tts_audio_ms          6    3120ms    3017ms    2960ms    3585ms
```

p50/p95 computed from the 12 (resp. 6, warmup dropped) raw samples:

- `stop_to_final_ms`: p50=1837ms, p95=1844ms (n=12; comparable to the
  2026-09-06 cell's n=10, p50=1771ms).
- `stop_to_tts_audio_ms` (echo mode, no LLM in the loop — same caveat as
  2026-09-06): p50=2968ms, p95=3585ms (n=6; lower than 2026-09-06's
  p50=3782ms — same isolated-session caveat as the TTS RTF number above).

First attempt at this V2V bench failed on every run
(`ConnectionResetError: [Errno 104] Connection reset by peer`, WS close code
4429) because the server's `SessionLimiter` (`effective_limit=1`) had a
session left in a non-clean state from an earlier `--server-loop` invocation
(a flag that expects the server to run its own LLM reply — this deployment
has none, so it errors every run and apparently never releases the session
slot). Fixed by `docker restart conversational-voice-speech`; the container
came back healthy and the retry (no `--server-loop`, using the default
echo-mode client-side reply) succeeded on the first attempt.

### Reproduce

```bash
# cat-remote, container running (docker-compose.rk3576.yml + .env.bak-voiceC,
# QWEN3_ASR_VAD_FINAL_ASYNC=0 overlay applied)
cd ~/ovs-ps-build-rk
python3 bench/perf/offline_asr_bench.py --host 127.0.0.1:8621 --lang zh --category short --limit 5
python3 bench/perf/offline_asr_bench.py --host 127.0.0.1:8621 --lang zh --category long --limit 5
python3 bench/perf/measure_v2v_unified.py --host 127.0.0.1:8621 \
    --wav bench/perf/corpus/short/zh_short_01.wav --runs 6 --warmup 1 --tts --multi 2 --language zh
# TTS RTF: POST /tts per bench/perf/corpus/tts_prompts.json zh_short_01..05,
# read x-rtf response header directly (no bundled script for this metric
# alone at time of writing; see boundary.zh.yaml's 2026-09-06 cell for the
# same method).
```

Caveats carry over unchanged from the 2026-09-06 run (single measurement
pass, not independently reproduced; V2V echo-mode is not a full
conversational round trip). Not reproduced by a second person.
